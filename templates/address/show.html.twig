{% extends 'base.html.twig' %}

{% block title %}Address{% endblock %}

{% block body %}
    <h1>Address</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ address.id }}</td>
            </tr>
            <tr>
                <th>Full_name</th>
                <td>{{ address.fullName }}</td>
            </tr>
            <tr>
                <th>Société_name</th>
                <td>{{ address.SocieteName }}</td>
            </tr>
            <tr>
                <th>Address</th>
                <td>{{ address.address }}</td>
            </tr>
            <tr>
                <th>Phone</th>
                <td>{{ address.phone }}</td>
            </tr>
            <tr>
                <th>Ville</th>
                <td>{{ address.ville }}</td>
            </tr>
            <tr>
                <th>Postal</th>
                <td>{{ address.postal }}</td>
            </tr>
            <tr>
                <th>Pays</th>
                <td>{{ address.pays }}</td>
            </tr>
            <tr>
                <th>Address_complement</th>
                <td>{{ address.AddressComplement }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('compte') }}">Retour page compte</a>

    <a href="{{ path('address_edit', {'id': address.id}) }}">Modifier</a>

    {{ include('address/_delete_form.html.twig') }}
{% endblock %}